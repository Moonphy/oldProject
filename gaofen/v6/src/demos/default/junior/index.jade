---
page_title: "广州站-初中频道-首页"
category: "default"
body_class: ["junior-page"]
stylesheets: ["exam_index"]
breadcrumb: ["首页", "学校信息", "华南师范大学附属中学"]
---

//- 模板继承
extends ../../_layout

//- 导入侧栏挂件
//- 挂件：日历
include ../../partials/widgets/_calendar

//- 导入图文列表
include ../../partials/_thumb-info-list-s2

//- 导入资料图文项
include ../../partials/_data-thumb-info-item

//- 内容
block content
  // Start:主导航
  +navbar(default_nav_links)
  // End:主导航

  // Start: 子导航
  include ../../partials/_sub-nav
  +sub-nav
  // End: 子导航

  +ad({width:980, ex_class:["container", "ad-top", "mt10"], count:3})

  .container.page-content
    .row.row13-7
      .span13

        // Start: 热门
        .mod.mod-hot
          .clearfix
            +ad({width:260, height:195, count:3})
            .rec-news
              .headline
                h3: a(href="#", target="_blank")= article.title
                p
                  != article.summary
              +post-list({classes:"post-list-large", subject:true, count:4})
        // End: 热门

        +ad({ex_class:["mb20"], width:620})

        // Start: 最新
        .mod.mod-latest
          +heading({title:"最新"})
          +thumb-info-list-s2({count:12, has_subject:true})
        // End: 最新
      .span7
        // Start: 学校查询
        +widget-school-query
        // End: 学校查询

        // Start: 历年分数线查询
        .widget.widget-query
          +heading({title:"历年分数线查询"})
          .bd
            .form-query
              +select({value:"2014", options:{count:5, text:"2014"}, selected:true, ex_class:["ui-select-mini"]})
              +select({value:"独立批", options:{count:5, text:"独立批"}, selected:true, ex_class:["ui-select-mini"]})
              +select({value:"全市", options:{count:5, text:"全市"}, selected:true, ex_class:["ui-select-mini"]})
              +button({option:"primary", dimension:"small", text:"查询"})
        // End: 历年分数线查询

        // Start: 挂件-日历
        +widget-calendar("中考")
        // End: 挂件-日历

        // Start: 挂件-中考活动
        +widget-event({title:"中考活动"})
        // End: 挂件-中考活动

        .widget.widget-data
          .heading.heading-tabs
            a.more(href="#", target="_blank") 更多
            .tabs
              span.active 热门资料
              span 最新资料
          .tab-content
            .thumb-data-list
              .list
                +data-thumb-info-item({pack:true, buy_now:true})
            +post-list({classes:["data-post"]})
          .tab-content.hidden
            +post-list({classes:["data-post"], count:10})

        +ad({ex_class:["mb20"], width:330})

        // Start: 社区精华
        +widget-post({title:"社区精华", ex_class:["widget-headline"], post_subject:true, post_class:"post-list-large", more:"更多", headline:true})
        // End: 社区精华

        // Start: 挂件: 微信
        +widget-weixin({type:"zk"})
        // End: 挂件: 微信

        // Start: 挂件: QQ群
        +widget-qq-group({type:"zk"})
        // End: 挂件: QQ群

    +ad({width:980, ex_class:["mb25"]})

    // Start: 资讯
    +link-cloud({link_title:"资讯"})
    // End: 资讯

    // Start: 友情链接
    +link-cloud
    // End: 友情链接